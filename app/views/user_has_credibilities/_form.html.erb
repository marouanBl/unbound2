<%= form_for(@user_has_credibility) do |f| %>
  <% if @user_has_credibility.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_has_credibility.errors.count, "error") %> prohibited this user_has_credibility from being saved:</h2>

      <ul>
      <% @user_has_credibility.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :users_id %><br />
    <%= f.number_field :users_id %>
  </div>
  <div class="field">
    <%= f.label :credibility_id %><br />
    <%= f.number_field :credibility_id %>
  </div>
  <div class="field">
    <%= f.label :credibility_articles_id %><br />
    <%= f.number_field :credibility_articles_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
